import type { ReactNode } from 'react';
interface CardProps {
    title?: string;
    children?: ReactNode;
    accent?: string;
}
export declare function Card({ title, children, accent }: CardProps): ReactNode;
export {};
//# sourceMappingURL=card.d.ts.map